{
  "name": "@redactor-ingestion/ingestion-service",
  "version": "1.0.0",
  "private": true,
  "description": "Microservice d'ingestion WordPress (articles, traductions, jobs)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --build",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@redactor-guide/ingestion-wp": "*",
    "dotenv": "^16.4.1",
    "fastify": "^5.2.0",
    "mongodb": "^6.3.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.19.30",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
