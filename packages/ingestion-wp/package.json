{
  "name": "@redactor-guide/ingestion-wp",
  "version": "1.0.0",
  "description": "Ingestion de contenu depuis WordPress (WPML)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@redactor-guide/core-model": "*",
    "mongodb": "^6.3.0",
    "turndown": "^7.2.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/turndown": "^5.0.6",
    "typescript": "^5.3.3"
  }
}
